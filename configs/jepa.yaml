# JEPA self-supervised encoder training
frames_path: "results/v0/frames.npz"
in_channels: 4
patch_size: 12
embed_dim: 192
num_heads: 3
num_layers: 4
feature_dim: 512
predictor_embed_dim: 96
predictor_num_heads: 2
predictor_num_layers: 2
mask_ratio: 0.5
ema_decay: 0.996
learning_rate: 1.5e-4
weight_decay: 0.05
epochs: 100
batch_size: 256
seed: 42
save_dir: "results/v0/jepa"
